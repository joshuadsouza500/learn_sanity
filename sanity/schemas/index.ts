import heroType from "./hero";
import serviceType from "./service";
import workType from "./work";
import reviewType from "./review";

export const schemaTypes = [heroType, serviceType, workType, reviewType];
